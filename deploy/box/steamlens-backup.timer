# Nightly at 03:30 UTC — the box's quiet hours. Persistent runs a missed
# backup at the next boot instead of skipping the day.
[Unit]
Description=steamlens nightly backup timer

[Timer]
OnCalendar=*-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
