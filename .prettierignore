# Human labeling surfaces are verbatim-sensitive: evidence spans must match
# review text byte-for-byte, and a markdown formatter rewriting emphasis
# (*x* -> _x_) or stripping trailing whitespace inside text fences silently
# breaks the copy-paste surface against the machine record (caught live
# 2026-07-17 on the gold workbook, again 2026-08-04 on the holdout sheet —
# hence the whole eval tree, not per-directory patches).
eval/
